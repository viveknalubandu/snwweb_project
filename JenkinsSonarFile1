pipeline {

  agent any
  tools {
       maven 'Maven'
   }
  stages {

       stage('SonarStage') {
       
	       environment {
	    		 SCANNER_HOME = tool 'sonarScanner'
	  		}
           steps {
              
             withSonarQubeEnv('sonarQube_local') {
			       sh '${SCANNER_HOME}/bin/sonar-scanner'
		       
        		}
	        
        	
           }
           
       }
 	
 	
 	}
}
